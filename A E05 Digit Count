#you are using python3
def countDigit(n):
    count = 0
    if(n==0):
        count=1
    else:
        while n != 0:
            n //= 10
            count += 1
    return count

n = int(input())
print(countDigit(n))
